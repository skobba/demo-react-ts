# Introduction 
A React frontend with typescript, babel and webpack.
# Run
> npm start

# Build (production)
> npm run build:prod

# Webpack Analyser
> npm run bundle:analyse
# Build Docker image
> ./buildDocker

# Run in docker-compose
> docker-compose up
